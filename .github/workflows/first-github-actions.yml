name: Run shell commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
     - name: Echo a string
       run: echo "hello srikant, this is my first github actions workflow file"
